export interface Transaction {
  transMerchant: string;
  transAmount: number;
  transDate: string;
  transCategory: string;
  transactionId: string;
  userId: string;
  transAccount: string;
  hidden: boolean;
} 